DRY (Don't Repeat Yourself)
Avoid duplicate code; keep logic in one place.

Use reusable functions, classes, or modules.

Benefits: easy maintenance, fewer bugs, better readability, consistency.

Related Principle
SRP (Single Responsibility Principle): One module = one responsibility

DRY + SRP → clean, modular, maintainable code

How to Apply DRY
Create functions/methods for repeated logic

Use classes/inheritance for reuse

Extract constants/configs

Follow modular design

Example
❌ Without DRY: duplicate functions
✔️ With DRY: single reusable function

If you want, I can convert all your system design topics into similar crisp 2–3 line interview notes 👍


Add c++ example code

Here’s a short DRY note with C++ example:

DRY (Don't Repeat Yourself)
Avoid duplicate code; keep logic in one place

Use reusable functions/classes

Benefits: maintainability, fewer bugs, consistency

Example (C++)
❌ Without DRY

bool validateEmail1(const string& email) {
    return email.find("@") != string::npos &&
           email.find(".") != string::npos;
}

bool validateEmail2(const string& email) {
    return email.find("@") != string::npos &&
           email.find(".") != string::npos;
}
✔️ With DRY

bool validateEmail(const string& email) {
    return email.find("@") != string::npos &&
           email.find(".") != string::npos;
}

int main() {
    string email = "test@example.com";

    if (validateEmail(email)) {
        cout << "Valid\n";
    }
}
